{
  "tasks": {
    "build": "mkdir -p build && cd build && cmake .. && make",
    "launch": "mkdir -p build && cd build && cmake .. && make && ./SteamdbCLI",
    "test": "mkdir -p build && cd build && cmake .. && make && make test"
  }
}